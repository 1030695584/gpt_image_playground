## v0.7.4（2026-08-13）

### 新增
- **多预置配置指定默认项**：部署端提供多个预置配置时，可通过 `isDefault: true` 指定首次导入及重新部署后自动选择的配置；默认项可参与拖拽排序，且只有一个预置项时会自动成为默认项。
- **预置配置保护策略**：新增部署端策略开关。可通过 `LOCK_PRESET_CONFIG_PARAMS` 锁定除 API Key 外的预置参数及供应商定义，或通过 `PREVENT_PRESET_CONFIG_DELETION` 禁止删除预置配置与供应商。
- **预置配置 Markdown 描述**：预置配置可通过可选的 `description` 字段提供 Markdown 说明，并在“当前配置”下方以说明卡片展示；内容支持选择、复制和点击链接。
- **sub2api 异步图片供应商**：新增内置的 sub2api（异步）供应商，支持异步提交图片生成与编辑任务、轮询任务状态，并在连接中断或应用重启后继续恢复任务结果。

### 变更
- **API URL 拼接规则支持跳过 /v1 前缀**：API URL 以 `/` 结尾时，请求将直接基于该地址拼接接口，不再自动添加 `/v1` 前缀；未以 `/` 结尾时继续遵循 OpenAI 兼容接口的默认补齐规则。该规则对所有供应商生效，并在配置导入、导出及分享链接中保持一致。
- **扩展 JSON 预置能力并支持无损更新原有配置**：原有的多配置 JSON 预置除自定义供应商外，现在也可混合包含 OpenAI 兼容和 fal.ai 配置。通过赋予配置稳定的 ID，在重新部署时可直接定位并无损更新用户本地的原有配置（默认仅同步最新 API 地址，保留用户填写的 API Key 和修改的模型、超时等参数）；被部署端移除的旧预置则会自动降级为普通配置。
- **预置配置文件支持打包内嵌**：使用远程或本地 `.json` 文件作为预置配置时，会在 Vite 构建阶段或 Docker 容器启动时自动读取并内嵌到静态文件中，用户的浏览器无需能够访问原始的配置源地址。
- **扩展“仅展示默认配置”模式**：原有的该模式已平滑升级为“仅使用预置配置”模式。在此模式下，不仅限制用户创建新配置，还支持用户在部署端提供的多个预置配置之间进行受控切换。
- **配置分享链接支持精准定位**：配置分享链接现在会携带配置 ID。再次打开同一链接时，可精准定位并更新对应的原有配置；同时支持在多预置环境的分享链接中，精确指定要应用的目标配置。

### 修复
- **流式 Images API 图片 URL 兼容**：OpenAI 兼容接口在 `image_generation.completed` 或 `image_edit.completed` 事件中返回图片 URL 时，现在会正确下载并识别最终图片；同时兼容流中的心跳注释和命名事件，并保留原始 URL 供下载失败时恢复。
- **Agent API 配置候选项筛选**：Agent 独立配置下拉框不再显示缺少 API Key 或有效 API URL 的配置；使用可用 API 代理或 fal.ai 默认地址的配置仍可正常选择，暂时不可用的原选择会在配置恢复后保留。
